<% if existing_action?(collection_presenter.resource_name, :edit) %>
  <td class="px-6 py-4">
    <%= link_to(
    t("administrate.actions.edit"),
    [:edit, namespace, resource],
    class: "font-medium text-primary dark:text-primary hover:underline",
  ) if accessible_action?(resource, :edit) %>
  </td>
<% end %>

<% if existing_action?(collection_presenter.resource_name, :destroy) %>
  <td class="px-6 py-4">
    <%= link_to(
    t("administrate.actions.destroy"),
    [namespace, resource],
    class: "font-medium text-graphite dark:text-muted hover:underline",
    data: confirm_delete_data(resource),
  ) if accessible_action?(resource, :destroy) %>
  </td>
<% end %>
