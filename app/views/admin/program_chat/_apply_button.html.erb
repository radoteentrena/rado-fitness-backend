<%= form_with url: apply_admin_program_chat_path(program), method: :post, class: "mb-2" do |f| %>
  <%= f.hidden_field :routine_id, value: routine_id %>
  <%= f.hidden_field :workout_id, value: workout_id %>
  <%= f.submit "✓ Aplicar cambios",
               class: "w-full bg-primary text-blackish font-bold text-sm py-2 rounded border-2 border-primary hover:bg-primary/90 transition-all",
               data: { turbo_submits_with: "Aplicando..." } %>
<% end %>
